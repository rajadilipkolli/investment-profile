micronaut:
  application:
    name: sipService
  server:
    port: 8084
netty:
  default:
    allocator:
      max-order: 3

eureka:
  client:
    registration:
      enabled: true
    defaultZone: "${EUREKA_HOST:localhost}:${EUREKA_PORT:8761}"
